syntax = "proto3";
option go_package="GRPC-TEST/math/mathpb";
package math;

service Math{
    rpc Sum(SumRequest) returns (SumResponse) {};
}

message SumRequest{
    int32 first_num = 1;
    int32 second_num = 2;
}

message SumResponse{
    int32 result = 1;
}